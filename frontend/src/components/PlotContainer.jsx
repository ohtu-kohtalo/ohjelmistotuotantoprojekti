import React from "react";

const PlotContainer = ({ data }) => (
  <div className="plot-container" data-testid="plot-container">
    <p>This is a plot container placeholder</p>
  </div>
);

export default PlotContainer;
